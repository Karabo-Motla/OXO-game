# Define the port number used for connecting to the server
PORT = 12345

# Define the buffer size for message transmission
BUFFER_SIZE = 32

# Define the format string for buffering messages, centered within the buffer size
BUFFER_STR = '{0:^' + str(BUFFER_SIZE) + '}'

# Define the game name, used for logging purposes
GAME_NAME = 'OXO'

# Define the board size for the game
BOARD_SIZE = 9
